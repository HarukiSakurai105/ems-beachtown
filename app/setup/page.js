'use client'
import { useEffect, useState } from 'react'
import Link from 'next/link'
import { useRouter } from 'next/navigation'
import { Activity, ArrowLeft, Check, Database, ShieldCheck, UserPlus } from 'lucide-react'
import LoadingScreen from '../../components/LoadingScreen'

export default function SetupPage() {
  const router = useRouter()
  const [form, setForm] = useState({ email: '', name: '', password: '', confirmPassword: '' })
  const [status, setStatus] = useState({ loading: true, saving: false, setupRequired: false, error: '' })
  useEffect(() => { fetch('/api/auth/session').then(r => r.json()).then(data => { if (data.user) return router.replace('/admin'); if (data.configured && !data.setupRequired) return router.replace('/login'); setStatus({ loading: false, saving: false, setupRequired: data.setupRequired, error: data.migrationRequired ? 'Hãy chạy migration 002_ems_users.sql trên Supabase trước.' : '' }) }).catch(() => setStatus(s => ({ ...s, loading: false, error: 'Không thể kiểm tra Supabase.' }))) }, [router])
  async function submit(event) { event.preventDefault(); if (form.password !== form.confirmPassword) return setStatus(s => ({ ...s, error: 'Hai mật khẩu không khớp.' })); setStatus(s => ({ ...s, saving: true, error: '' })); const response = await fetch('/api/auth/setup', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(form) }); const data = await response.json(); if (!response.ok) return setStatus(s => ({ ...s, saving: false, error: data.error })); router.replace('/admin'); router.refresh() }
  const checks = [{ icon: Database, text: 'Lưu vĩnh viễn trên Supabase' }, { icon: ShieldCheck, text: 'Tự động nhận quyền Admin' }, { icon: Check, text: 'Trang thiết lập tự khóa sau khi tạo' }]
  return <main id="main-content" className="mdt-auth min-h-screen px-4 py-8 text-[var(--ink)] sm:px-6"><LoadingScreen /><div className="mx-auto max-w-6xl"><Link href="/" className="mdt-mono inline-flex items-center gap-2 text-[10px] font-bold text-[var(--muted)] hover:text-[#8ce04b]"><ArrowLeft className="h-4 w-4" /> RETURN / PUBLIC MDT</Link><div className="mdt-panel mt-6 grid overflow-hidden lg:grid-cols-[.8fr_1.2fr]"><section className="border-b border-[var(--line)] bg-[#0c1115] p-7 lg:border-b-0 lg:border-r lg:p-10"><div className="flex items-center gap-3"><span className="grid h-11 w-11 place-items-center border border-[#8ce04b] bg-[#182614] text-[#8ce04b]"><Activity className="h-5 w-5" /></span><span><b className="mdt-display block text-base">BEACH TOWN EMS</b><small className="mdt-mono text-[8px] uppercase tracking-[.18em] text-[var(--muted)]">INITIAL SECURITY SETUP</small></span></div><p className="mdt-kicker mt-12">// BƯỚC KHỞI TẠO DUY NHẤT</p><h1 className="mdt-display mt-3 text-3xl">Thiết lập quản trị viên đầu tiên</h1><p className="mt-4 text-xs leading-6 text-[var(--muted)]">Tài khoản này sẽ quản lý nội dung, nhân sự và quyền truy cập của hệ thống.</p><div className="mt-8 space-y-2">{checks.map(item => <div key={item.text} className="mdt-control flex items-center gap-3 p-3 text-xs font-bold"><item.icon className="h-4 w-4 text-[#8ce04b]" />{item.text}</div>)}</div></section><section className="p-6 sm:p-8 lg:p-10"><div className="flex items-center gap-3"><span className="grid h-11 w-11 place-items-center border border-[#3fa9f5] bg-[#0f2536] text-[#3fa9f5]"><UserPlus className="h-5 w-5" /></span><div><p className="mdt-kicker">// ADMINISTRATOR PROFILE</p><h2 className="mdt-display mt-1 text-xl">Thông tin tài khoản</h2></div></div>{status.loading ? <p className="mt-10 text-sm text-[var(--muted)]">Đang kiểm tra kết nối Supabase…</p> : <form onSubmit={submit} className="mt-8 grid gap-4 sm:grid-cols-2"><SetupField label="Tên hiển thị"><input required value={form.name} onChange={e => setForm({ ...form, name: e.target.value })} className="setup-input" placeholder="Viện trưởng EMS" /></SetupField><SetupField label="Email đăng nhập"><input required type="email" value={form.email} onChange={e => setForm({ ...form, email: e.target.value })} className="setup-input" placeholder="admin@example.com" /></SetupField><SetupField label="Mật khẩu"><input required type="password" minLength={12} value={form.password} onChange={e => setForm({ ...form, password: e.target.value })} className="setup-input" placeholder="Tối thiểu 12 ký tự" /></SetupField><SetupField label="Xác nhận mật khẩu"><input required type="password" minLength={12} value={form.confirmPassword} onChange={e => setForm({ ...form, confirmPassword: e.target.value })} className="setup-input" /></SetupField>{status.error && <p role="alert" className="border border-[#ffc530] bg-[#332905] p-3 text-xs text-[#ffc530] sm:col-span-2">{status.error}</p>}<button disabled={status.saving || !status.setupRequired} className="mdt-mono inline-flex items-center justify-center gap-2 border border-[#8ce04b] bg-[#182614] py-3.5 text-xs font-bold uppercase text-[#8ce04b] hover:bg-[#24381c] disabled:opacity-40 sm:col-span-2"><ShieldCheck className="h-4 w-4" />{status.saving ? 'INITIALIZING…' : 'CREATE / ADMIN NODE'}</button></form>}</section></div></div></main>
}
function SetupField({ label, children }) { return <label className="setup-label">{label}{children}</label> }
